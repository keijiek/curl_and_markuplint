# curl_and_markuplint

- clone直後は次を実行

```bash
npm i
```

- 実行環境は powershell。
- 【重要】curl_urls.ps1 の実行はフルパスで。
- 実行すると、実行日時に由来するディレクトリが作成され、その中に、curlしたurlのソースコードがhtmlファイルに出力される。
- 実行後、出力したファイルをmarkuplintにかけるためのコマンドがクリップされる。
